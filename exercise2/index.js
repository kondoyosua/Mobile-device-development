import { fahrenheit, celcius} from "./konversi.js";
console.log(`${fahrenheit.toFixed(2)} Fahrenheit = ${celcius.toFixed(2)} Celcius`);

import { weight, height } from "./konversi.js"
import { bmi, category } from "./konversi.js";
import calculateBMI from "./konversi.js";
console.log(`Your BMI is ${bmi.toFixed(2)}, which means you are ${category}.`);
